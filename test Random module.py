import random
random.seed(5)
print(random.random())
# dùng hàm random.seed(giá trị nguyên ví dụ là 15) và sinh số ngẫu nhiên được giá trị a 
# Khi chạy lại random.seed(15) nó sẽ lại ra giá trị a => Mục đích là kiểm chứng với giá trị của bạn là abc thì sẽ ra kết quả xyz... 
# Người khác kiểm tra chính xác giá trị của bạn
